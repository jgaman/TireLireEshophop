@model IEnumerable<TireLireEshop.Image>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CheminDAcces)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdProduitNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CheminDAcces)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdProduitNavigation.DescriptionParLeFabricant)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PkIdImage">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PkIdImage">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PkIdImage">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
