@model IEnumerable<TireLireEshop.Commandes>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdClientNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdStatutCommandeNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdClientNavigation.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdStatutCommandeNavigation.StatutCommande1)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PkIdCommande">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PkIdCommande">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PkIdCommande">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
